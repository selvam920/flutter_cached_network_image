name: example
description: A project that showcases usage of cached_network_image
publish_to: none
version: 1.0.0+1
environment:
  sdk: ^3.0.0
  flutter: '>=3.10.0'

dependencies:
  baseflow_plugin_template: ^2.1.2
  cached_network_image:
    path: ../
  flutter:
    sdk: flutter
  flutter_blurhash:
    git:
      url: https://github.com/braverhealth/flutter_blurhash

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter:
  uses-material-design: true
